#include <iostream>
using namespace std;
int mult(int ,int );
int main() 
{
  int x;
  x=mult(2,7);
  cout<<x;
}
int mult(int x,int y)
{
  int i, sumis=0;
  for(i=1;i<=y;i++)
  {
    sumis=sumis+x;
  }
return sumis;
}
